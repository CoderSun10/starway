# 桌面端只从构建时注入的 VITE_API_BASE_URL 读 API 地址（设置页不再填写）。
# 按环境自动区分，见同目录：
#   .env.development  开发（npm run dev / dev:web）→ http://127.0.0.1:3001
#   .env.production   打包发行（vite build / dist:win / dist:linux）→ 线上 API
# 这两个文件随仓库提交。本文件是个人本地覆盖，可选：
# 临时指向别的后端时复制本文件为 .env 并取消下面注释，它只影响你这台机器。
# VITE_API_BASE_URL=
